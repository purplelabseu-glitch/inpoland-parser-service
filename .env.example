# Скопируйте PROXY_* / PROXY_REFRESH_URL с mobilede-parser-service (.env на VPS).

PROXY_URL=http://user:password@proxy-host:port
# PROXY_REFRESH_URL=https://api.asocks.com/proxy/<id>/refresh-ip
BROWSER_MAX_RETRIES=3
REFRESH_WAIT_S=4

# auto = Chromium первым (Cloudflare + cookies), потом Camoufox
BROWSER_ENGINE=auto
BROWSER_HEADLESS=true
BROWSER_LOCALE=ru-RU
NAV_TIMEOUT_MS=90000
CHALLENGE_WAIT_S=60
CONTENT_WAIT_S=45
BROWSER_CONCURRENCY=1

# Cloudflare cookies (cf_clearance и т.п.) — Playwright storage_state
STORAGE_STATE_PATH=.cache/inpoland-storage.json
STICKY_CHROMIUM=true
STICKY_PROXY=true

COLLECT_MAX_PAGES_DEFAULT=10
COLLECT_MAX_PAGES_LIMIT=20

API_KEY=

LOG_LEVEL=INFO
